package com.sweak.qralarm.core.domain.user

// Update to the current versionCode when shipping new "What's new" content.
// Keep unchanged for versions with no new content — returning users won't see a repeat.
const val WHATS_NEW_VERSION_CODE = 82

val EMERGENCY_DEFAULT_REQUIRED_MATCHES_RANGE = 0..100
const val EMERGENCY_DEFAULT_REQUIRED_MATCHES = 50
